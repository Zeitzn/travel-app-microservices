spring:
  application:
    name: microservice-gateway
  cloud:
    config:
      enabled: true
      uri: http://localhost:8083 #Puerto de microservice-config
      username: root
      password: s3cr3t
  profiles:
    active:
    - dev